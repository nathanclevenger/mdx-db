// Constants for vector operations
export const EPSILON = 1e-10; // Tolerance for floating point comparisons
export const DEFAULT_VECTOR_CONFIG = {
    dimensions: 256,
    metric: 'cosine',
    normalize: true
};
// Validation constants
export const VECTOR_VALIDATION = {
    MIN_DIMENSIONS: 1,
    MAX_DIMENSIONS: 4096,
    MIN_THRESHOLD: 0,
    MAX_THRESHOLD: 1
};
//# sourceMappingURL=constants.js.map